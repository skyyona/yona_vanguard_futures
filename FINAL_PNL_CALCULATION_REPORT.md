# P&L % 계산 방식 최종 정리 보고서

## 📋 목적

사용자가 제시한 예시를 바탕으로 P&L % 계산 방식을 정확하게 이해하고 검증합니다.

---

## 🎯 사용자 제시 예시 상세 분석

### 예시 1: 앱 초기 실행 시

```
- Initial Investment = 60 USDT
- Account total balance = 60 USDT
- P&L % = 0.00 %
```

**분석**:
- 초기 상태에서 P&L %는 0%로 시작
- Initial Investment와 Account total balance가 동일
- 아직 거래나 배분이 없으므로 손익 없음

**계산 검증**:
```
P&L % = ((60 - 60) / 60) * 100 = 0.00 % ✅
또는
P&L % = (0 / 60) * 100 = 0.00 % ✅
```

---

### 예시 2: 각 엔진 배분 시

```
- 각 엔진 거래 진행을 위한 'Account total balance' 총 사용자 자금을 
  'Designated Funds' 슬라이더 바로 배분 시
- 'P&L %'은 기존 및 이전 'Initial Investment' 초기 사용자 투입 자금 기준 표기 = '0.00 %'
```

**분석**:
- 배분만 했을 때는 P&L %가 **0%로 유지됨**
- 배분은 손익이 아니므로 P&L %에 영향을 주지 않음
- Account total balance는 배분에 따라 차감되지만, P&L %는 0% 유지

**계산 검증**:
```
Initial Investment = 60 USDT

1. Alpha 30% 배분: 18 USDT
   Account total balance = 60 - 18 = 42 USDT
   실현 손익 = 0 USDT
   P&L % = (0 / 60) * 100 = 0.00 % ✅

2. Beta 30% 배분: 12.6 USDT (42 × 30%)
   Account total balance = 42 - 12.6 = 29.4 USDT
   실현 손익 = 0 USDT
   P&L % = 0.00 % ✅

3. Gamma 30% 배분: 8.82 USDT (29.4 × 30%)
   Account total balance = 29.4 - 8.82 = 20.58 USDT
   실현 손익 = 0 USDT
   P&L % = 0.00 % ✅
```

**핵심**: 배분은 손익이 아니므로 P&L %에 영향을 주지 않음 ✅

---

### 예시 3: 각 엔진 거래 종료 후 수익/손실 추가 시

```
- 각 엔진 거래 종료 후 수익/손실 추가 시
- 'P&L %'은 기존 및 이전 'Initial Investment' + 확정된 'Account total balance' 총 사용자 자금에 대한 'P&L %' 표기
```

**표현 해석**:
"Initial Investment + 확정된 Account total balance 총 사용자 자금에 대한 P&L %"

이 표현을 해석하면:
- Initial Investment 기준으로 계산
- Account total balance는 실현 손익이 반영된 후의 값
- "확정된"이라는 단어는 실현 손익을 의미

**가장 합리적인 해석**:

```
P&L % = ((Account total balance - Initial Investment) / Initial Investment) * 100

단, Account total balance = Initial Investment - 배분 합계 + 실현 손익 합계
```

**계산 검증**:
```
Initial Investment = 60 USDT
배분: Alpha 18 + Beta 12.6 + Gamma 8.82 = 39.42 USDT

1. 배분만 했을 때:
   Account total balance = 60 - 39.42 = 20.58 USDT
   실현 손익 = 0 USDT
   P&L % = ((20.58 - 60) / 60) * 100 = -65.7% ❌
   
   하지만 사용자 의도: P&L % = 0.00 % ✅
   
   → 문제: 배분 차감을 손익으로 간주하는 것이 부적절
```

**올바른 해석**:

사용자 표현 "Initial Investment + 확정된 Account total balance 총 사용자 자금에 대한 P&L %"를 다시 해석하면:

**해석 A**: Account total balance 기준이지만, 배분은 손익이 아니므로 제외
```
Account total balance = Initial Investment - 배분 합계 + 실현 손익 합계

실질적 가치 = Initial Investment + 실현 손익 합계
(배분은 단순 할당이므로 실질적 가치에 영향 없음)

P&L % = (실현 손익 합계 / Initial Investment) * 100
```

**계산 검증 (해석 A)**:
```
Initial Investment = 60 USDT

1. 배분만:
   Account total balance = 60 - 39.42 = 20.58 USDT
   실현 손익 = 0 USDT
   P&L % = (0 / 60) * 100 = 0.00 % ✅

2. Alpha 거래 종료: +10 USDT
   Account total balance = 20.58 + 10 = 30.58 USDT
   실현 손익 = +10 USDT
   P&L % = (10 / 60) * 100 = +16.67 % ✅
```

---

## ✅ 최종 해석: 사용자 의도 정리

### 사용자 의도 최종 정리

1. **배분만 했을 때**: 
   - P&L % = 0.00 % (배분은 손익이 아니므로)

2. **거래 종료 후**: 
   - 실현 손익이 Account total balance에 추가됨
   - P&L %는 실현 손익 기준으로 계산됨
   - Initial Investment 기준으로 계산

3. **계산 방식**:
   ```
   P&L % = (실현 손익 합계 / Initial Investment) * 100
   ```

---

## 📊 최종 계산 방식 (사용자 의도 반영)

### 계산 공식 (최종)

```
Account total balance = Initial Investment - 배분 합계 + 실현 손익 합계

P&L % = (실현 손익 합계 / Initial Investment) * 100
```

### 전체 예시 (60 USDT 기준)

```
Initial Investment = 60 USDT

1. 초기 상태:
   Account total balance = 60 USDT
   배분 합계 = 0 USDT
   실현 손익 = 0 USDT
   P&L % = (0 / 60) * 100 = 0.00 % ✅

2. Alpha 30% 배분: 18 USDT
   Account total balance = 60 - 18 = 42 USDT
   배분 합계 = 18 USDT
   실현 손익 = 0 USDT
   P&L % = (0 / 60) * 100 = 0.00 % ✅

3. Beta 30% 배분: 12.6 USDT (42 × 30%)
   Account total balance = 42 - 12.6 = 29.4 USDT
   배분 합계 = 18 + 12.6 = 30.6 USDT
   실현 손익 = 0 USDT
   P&L % = 0.00 % ✅

4. Gamma 30% 배분: 8.82 USDT (29.4 × 30%)
   Account total balance = 29.4 - 8.82 = 20.58 USDT
   배분 합계 = 30.6 + 8.82 = 39.42 USDT
   실현 손익 = 0 USDT
   P&L % = 0.00 % ✅

5. Alpha 거래 종료: +10 USDT 수익 실현
   Account total balance = 20.58 + 10 = 30.58 USDT
   배분 합계 = 39.42 USDT
   실현 손익 = +10 USDT
   P&L % = (10 / 60) * 100 = +16.67 % ✅

6. Beta 거래 종료: -3 USDT 손실 실현
   Account total balance = 30.58 - 3 = 27.58 USDT
   배분 합계 = 39.42 USDT
   실현 손익 = +10 - 3 = +7 USDT
   P&L % = (7 / 60) * 100 = +11.67 % ✅

7. Gamma 거래 종료: +5 USDT 수익 실현
   Account total balance = 27.58 + 5 = 32.58 USDT
   배분 합계 = 39.42 USDT
   실현 손익 = +10 - 3 + 5 = +12 USDT
   P&L % = (12 / 60) * 100 = +20.00 % ✅
```

---

## 🔍 이전 계산과의 비교

### 이전 계산 (문제 있음)

```
1. Alpha 배분: P&L % = -30.0% ❌
2. Alpha 거래 종료 (+10): P&L % = -60.7% ❌
```

**문제점**:
- 배분만 했을 때 손익률 발생
- 수익이 발생했는데도 손실로 표시

---

### 최종 계산 (올바름)

```
1. Alpha 배분: P&L % = 0.00% ✅
2. Alpha 거래 종료 (+10): P&L % = +16.67% ✅
```

**개선점**:
- 배분만 했을 때 손익률 0% (올바름)
- 거래 종료 시 실제 수익 정확히 반영

---

## ✅ 최종 사용자 의도 정리

### 1. Initial Investment
- 앱 실행 시 자동 설정, 재시작 시 변경

### 2. Account total balance
- 배분 시 차감 표기
- 거래 종료 시 수익/손실 추가

### 3. P&L % (헤더)

**계산 공식**:
```
P&L % = (실현 손익 합계 / Initial Investment) * 100
```

**특징**:
- 배분만 했을 때: P&L % = 0.00 % ✅
- 거래 종료 시: 실현 손익이 추가되면 P&L % 업데이트 ✅
- Initial Investment 기준으로 계산 ✅

### 4. 각 엔진의 Total Slot Gain/Loss 및 P&L %
- 해당 엔진의 Designated Funds 기준으로 표기

---

## 📊 검증 완료 사항

### ✅ 올바른 계산 방식

1. **배분만 했을 때**: P&L % = 0.00 % ✅
2. **거래 종료 시**: 실현 손익 기준으로 계산 ✅
3. **Initial Investment 기준**: 모든 계산이 Initial Investment 기준 ✅

### ❌ 이전 계산의 문제점 (수정됨)

1. 배분만 했을 때 손익률 발생 → 수정: 0%로 유지
2. 거래 종료 시 손익률 부정확 → 수정: 실현 손익만 반영

---

## 📝 최종 구현 방안

### P&L % 계산 로직

```python
# 실현 손익 합계 계산
total_realized_pnl = sum(engine.realized_pnl for engine in engines)

# P&L % 계산
if initial_investment > 0:
    pnl_percent = (total_realized_pnl / initial_investment) * 100.0
else:
    pnl_percent = 0.0
```

### Account total balance 계산 로직

```python
# 배분 합계 계산
total_allocated = sum(engine.designated_funds for engine in engines)

# 실현 손익 합계 계산
total_realized_pnl = sum(engine.realized_pnl for engine in engines)

# Account total balance 계산
account_total_balance = initial_investment - total_allocated + total_realized_pnl
```

---

## ✅ 결론

### 사용자 의도 최종 정리

1. **배분 시**: P&L % = 0.00 % (배분은 손익이 아니므로)
2. **거래 종료 시**: P&L % = (실현 손익 합계 / Initial Investment) * 100
3. **Initial Investment 기준**: 모든 계산이 Initial Investment 기준

### 계산 공식 (최종)

```
Account total balance = Initial Investment - 배분 합계 + 실현 손익 합계

P&L % = (실현 손익 합계 / Initial Investment) * 100
```

### 예시 검증

```
Initial Investment = 60 USDT

초기: Account total balance = 60 USDT, P&L % = 0.00 % ✅
배분만: Account total balance = 20.58 USDT, P&L % = 0.00 % ✅
거래 종료 (+10): Account total balance = 30.58 USDT, P&L % = +16.67 % ✅
최종 (+12): Account total balance = 32.58 USDT, P&L % = +20.00 % ✅
```

---

**검증 일시**: 2025-01-XX  
**검증자**: AI Assistant  
**검증 범위**: 사용자 제시 예시 기반 P&L % 계산 방식 최종 검증


