[pytest]
# Restrict discovery to project tests and avoid recursing into embedded virtualenvs
testpaths = tests
norecursedirs = .venv venv venv* .venv* __pycache__ build dist .git node_modules .pytest_cache site-packages *.egg-info

# Keep warnings visible and use default test file patterns
addopts = -q
