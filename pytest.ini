[pytest]
markers =
    integration: marks tests as integration (deselect with -m "not integration")

# Note: `asyncio_mode` option caused warnings in some pytest/plugin versions.
# We rely on `pytest-asyncio` plugin defaults. If you need to force a mode,
# add it to your local config or ensure a compatible `pytest-asyncio` version is installed.
