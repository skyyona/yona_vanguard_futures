# 백테스팅 시스템 통합 가능성 검증 보고서

**생성일**: 2025-11-20  
**검증 대상**: 실시간 랭킹리스트 → 백테스팅 → 거래 적합성 평가 기능

---

## 📋 사용자 요구사항 분석

### 요구사항 요약
```
사용자가 중단 세션의 1열 '실시간 랭킹리스트'의 
'링크' 표기 항목의 열에서 
검색하는 '코인 심볼'의 '상승률%', '누적', '상승 유형' 해당 행을 클릭

→ 우리 앱 엔진의 전략에 적합한지를 
   해당 '코인 심볼'의 1주 혹은 한달 간의 데이터로 백테스팅 진행

→ 해당 '코인 심볼'의 선물 거래 진행에 '적합' 또는 '부적합' 표기

→ '랭크' 항목의 명칭을 '백테스팅' 혹은 '거래 적합성'으로 변경
```

### 현재 UI 구조 (ranking_table_widget.py)

#### 현재 컬럼 구조
```python
["선택", "랭크", "코인 심볼", "상승률%", "누적", "상승 유형"]
#   0      1        2          3         4        5
```

#### 현재 클릭 동작 (line 267-272)
```python
if col == 2:  # 심볼 컬럼 - 바이낸스 페이지 열기
    url = symbol_widget.property("url")
    webbrowser.open(url)
    
elif col in [3, 4, 5]:  # 상승률/누적/유형 - 분석 요청
    self.analyze_requested.emit(symbol)
```

### 현재 분석 플로우 (main.py)

#### 1. 분석 요청 흐름
```
랭킹 테이블 클릭 (col 3/4/5)
    ↓
analyze_requested.emit(symbol)
    ↓
_on_analyze_symbol(symbol)
    ↓
/api/v1/live/analysis/entry?symbol={symbol}
    ↓
우측 2열 "Coin Momentum & Chart" 업데이트
```

#### 2. 현재 분석 API
- **엔드포인트**: `/api/v1/live/analysis/entry`
- **용도**: 실시간 진입 타이밍 분석 (5m/15m 추세, 점수, 레벨)
- **백테스트 기능**: ❌ 없음

---

## 🔍 기존 백테스트 시스템 현황

### 1. BacktestAdapter 구현 완료

**위치**: `backend/core/new_strategy/backtest_adapter.py`

#### 핵심 기능
```python
class BacktestAdapter:
    def run_backtest(self, orchestrator, config: BacktestConfig) -> Dict[str, Any]:
        """
        백테스트 실행
        
        Returns:
            {
                "total_pnl": float,           # 총 수익 (USDT)
                "total_pnl_pct": float,       # 수익률 (%)
                "total_trades": int,          # 총 거래 횟수
                "win_rate": float,            # 승률 (%)
                "winning_trades": int,        # 승리 거래
                "losing_trades": int,         # 손실 거래
                "avg_win": float,             # 평균 수익
                "avg_loss": float,            # 평균 손실
                "profit_factor": float,       # 손익비
                "max_drawdown": float,        # 최대 낙폭 (%)
                "sharpe_ratio": float,        # 샤프 비율
                "trades": List[Dict],         # 거래 내역
                "equity_curve": List[Dict]    # 자산 곡선
            }
        """
```

#### 주요 클래스
1. **BacktestConfig**: 백테스트 설정
   - symbol, start_date, end_date
   - initial_balance, leverage
   - commission_rate, slippage_rate

2. **BacktestDataLoader**: 과거 데이터 로드
   - Binance API에서 1m/3m/15m 캔들 조회
   - DataFrame 변환

3. **SimulatedPosition**: 시뮬레이션 포지션
   - 슬리피지 & 수수료 자동 적용
   - PNL 계산

4. **BacktestExecutor**: 백테스트 실행
   - Orchestrator와 연동
   - 진입/청산 시뮬레이션
   - 성능 메트릭 계산

### 2. 백테스트 예제 스크립트

**위치**: `backtest_metusdt_today.py` (353 lines)

#### 실행 사례
```python
# 오늘(00:00~현재) METUSDT 백테스트
# 진입 조건, 리스크 관리, 손절/익절 검증
```

**결과 출력 예시**:
```
총 거래: 5회
승률: 60.0%
총 수익: +123.45 USDT (+1.23%)
최대 낙폭: -2.5%
```

---

## ✅ 구현 가능성 평가

### 1. 백엔드 구현 가능성: **✅ 100% 가능**

#### 이유
1. ✅ **BacktestAdapter 이미 완성**: 완전한 백테스트 엔진 구현됨
2. ✅ **Orchestrator 통합**: Alpha/Beta/Gamma 전략과 동일한 로직 사용
3. ✅ **과거 데이터 로드**: Binance API 연동 완료
4. ✅ **성능 메트릭 계산**: 승률, 수익률, MDD, Sharpe 등 모든 지표 지원

#### 필요 작업
```python
# 새 API 엔드포인트 추가
# backend/app_main.py 또는 별도 라우터

@app.get("/api/v1/backtest/suitability")
async def get_trading_suitability(symbol: str, period: str = "1w"):
    """
    코인 심볼의 거래 적합성 평가
    
    Args:
        symbol: 코인 심볼 (예: "GRASSUSDT")
        period: 백테스트 기간 ("1w" or "1m")
    
    Returns:
        {
            "symbol": "GRASSUSDT",
            "period": "1w",
            "suitability": "적합" | "부적합",
            "score": 75.5,  # 0~100 점수
            "reason": "승률 60%, 수익률 +5.2%",
            "metrics": {
                "total_pnl_pct": 5.2,
                "win_rate": 60.0,
                "total_trades": 10,
                "max_drawdown": 3.5,
                "sharpe_ratio": 1.2
            }
        }
    """
    # 1. 기간 계산
    end_date = datetime.now()
    if period == "1w":
        start_date = end_date - timedelta(days=7)
    else:  # "1m"
        start_date = end_date - timedelta(days=30)
    
    # 2. BacktestConfig 생성
    config = BacktestConfig(
        symbol=symbol,
        start_date=start_date.strftime("%Y-%m-%d"),
        end_date=end_date.strftime("%Y-%m-%d"),
        initial_balance=10000.0,
        leverage=50,
    )
    
    # 3. Orchestrator 생성 (Alpha 전략 사용)
    orchestrator = StrategyOrchestrator(
        binance_client=shared_binance_client,
        config=OrchestratorConfig(symbol=symbol, leverage=50)
    )
    
    # 4. 백테스트 실행
    adapter = BacktestAdapter(shared_binance_client)
    results = adapter.run_backtest(orchestrator, config)
    
    # 5. 적합성 판단 로직
    suitability, score = evaluate_suitability(results)
    
    return {
        "symbol": symbol,
        "period": period,
        "suitability": suitability,
        "score": score,
        "reason": generate_reason(results),
        "metrics": results
    }

def evaluate_suitability(results: Dict) -> Tuple[str, float]:
    """
    백테스트 결과로 거래 적합성 판단
    
    판단 기준:
    - 승률 >= 50%
    - 수익률 >= +2%
    - 최대 낙폭 <= 5%
    - 거래 횟수 >= 3회 (최소 샘플)
    """
    score = 0.0
    
    # 승률 (30점)
    win_rate = results.get("win_rate", 0)
    if win_rate >= 70:
        score += 30
    elif win_rate >= 50:
        score += 20
    elif win_rate >= 40:
        score += 10
    
    # 수익률 (40점)
    pnl_pct = results.get("total_pnl_pct", 0)
    if pnl_pct >= 5:
        score += 40
    elif pnl_pct >= 2:
        score += 30
    elif pnl_pct >= 0:
        score += 15
    
    # 최대 낙폭 (20점)
    mdd = results.get("max_drawdown", 100)
    if mdd <= 3:
        score += 20
    elif mdd <= 5:
        score += 15
    elif mdd <= 10:
        score += 10
    
    # 거래 횟수 (10점)
    trades = results.get("total_trades", 0)
    if trades >= 5:
        score += 10
    elif trades >= 3:
        score += 5
    
    # 적합성 판단
    if score >= 70 and win_rate >= 50 and pnl_pct >= 2:
        suitability = "적합"
    elif score >= 50:
        suitability = "주의 필요"
    else:
        suitability = "부적합"
    
    return suitability, score
```

### 2. GUI 통합 가능성: **✅ 100% 가능**

#### 필요 수정 사항

##### A. 컬럼 구조 변경 (ranking_table_widget.py)

**현재**:
```python
["선택", "랭크", "코인 심볼", "상승률%", "누적", "상승 유형"]
```

**변경 후**:
```python
["선택", "거래 적합성", "코인 심볼", "상승률%", "누적", "상승 유형"]
#          ^^^^^^^^^ "랭크" → "거래 적합성"
```

##### B. 거래 적합성 표시 로직

**데이터 구조**:
```python
item = {
    "symbol": "GRASSUSDT",
    "change_percent": 15.5,
    "cumulative_percent": 25.3,
    "energy_type": "급등",
    
    # 새로 추가
    "backtest_status": "적합" | "부적합" | "분석중" | "대기",
    "backtest_score": 75.5,  # 0~100
    "backtest_period": "1w"  # or "1m"
}
```

**UI 렌더링**:
```python
# 거래 적합성 (컬럼 1)
status = item.get("backtest_status", "대기")
score = item.get("backtest_score", 0)

if status == "적합":
    suitability_text = f"✅ 적합 ({score:.0f}점)"
    color = "#03b662"  # 녹색
elif status == "부적합":
    suitability_text = f"❌ 부적합 ({score:.0f}점)"
    color = "#e16476"  # 빨강
elif status == "분석중":
    suitability_text = "⏳ 분석중..."
    color = "#1e88e5"  # 파랑
else:  # "대기"
    suitability_text = "-"
    color = "#3c3c3c"  # 회색

suitability_item = QTableWidgetItem(suitability_text)
suitability_item.setTextAlignment(Qt.AlignCenter)
suitability_item.setForeground(QColor(color))
self.setItem(i, 1, suitability_item)
```

##### C. 클릭 이벤트 처리

**현재**:
```python
elif col in [3, 4, 5]:  # 상승률/누적/유형 - 분석 요청
    self.analyze_requested.emit(symbol)
```

**변경 후**:
```python
elif col == 1:  # 거래 적합성 - 백테스트 시작/결과 표시
    if item.get("backtest_status") == "대기":
        # 백테스트 시작
        self.backtest_requested.emit(symbol)
    else:
        # 백테스트 결과 상세 표시
        self.backtest_detail_requested.emit(symbol)

elif col in [3, 4, 5]:  # 상승률/누적/유형 - 기존 분석 유지
    self.analyze_requested.emit(symbol)
```

##### D. 백테스트 요청 플로우 (main.py)

```python
# Signal 추가
class RankingTableWidget(QTableWidget):
    backtest_requested = Signal(str)  # 백테스트 시작 요청
    backtest_detail_requested = Signal(str)  # 결과 상세 보기

# main.py에서 연결
self.ranking_table.backtest_requested.connect(self._on_backtest_requested)
self.ranking_table.backtest_detail_requested.connect(self._on_backtest_detail)

def _on_backtest_requested(self, symbol: str):
    """백테스트 시작 요청"""
    print(f"[MAIN] 백테스트 시작 요청: {symbol}")
    
    # 1. UI 상태 변경 (컬럼 1을 "분석중"으로)
    self._update_backtest_status(symbol, "분석중", 0)
    
    # 2. 백그라운드에서 백테스트 API 호출
    def worker():
        try:
            response = requests.get(
                f"{BASE_URL}/api/v1/backtest/suitability",
                params={"symbol": symbol, "period": "1w"},
                timeout=30  # 백테스트는 시간이 걸릴 수 있음
            )
            
            if response.ok:
                data = response.json()
                suitability = data.get("suitability", "부적합")
                score = data.get("score", 0)
                
                # UI 업데이트 (Signal 사용)
                self.backtest_completed.emit(symbol, suitability, score, data)
            else:
                self.backtest_failed.emit(symbol, "API 오류")
        
        except Exception as e:
            self.backtest_failed.emit(symbol, str(e))
    
    threading.Thread(target=worker, daemon=True).start()

def _on_backtest_detail(self, symbol: str):
    """백테스트 결과 상세 보기"""
    # 팝업 다이얼로그로 상세 메트릭 표시
    # - 총 거래 횟수
    # - 승률
    # - 수익률
    # - 최대 낙폭
    # - 샤프 비율
    # - 거래 내역 목록
    pass
```

---

## 📊 UI/UX 설계

### 1. 백테스트 실행 플로우

```
사용자가 "거래 적합성" 컬럼 클릭 (상태: "대기")
    ↓
"⏳ 분석중..." 표시
    ↓
백엔드 API 호출 (/api/v1/backtest/suitability)
    ↓ (7일 간 데이터 로드 & 백테스트 실행)
    ↓ (약 5~15초 소요)
    ↓
결과 수신
    ↓
"✅ 적합 (75점)" 또는 "❌ 부적합 (45점)" 표시
```

### 2. 컬럼 레이아웃

```
┌──────┬────────────┬────────────┬──────────┬────────┬────────────┐
│ 선택 │ 거래 적합성 │ 코인 심볼   │ 상승률%  │  누적  │ 상승 유형  │
├──────┼────────────┼────────────┼──────────┼────────┼────────────┤
│  ☐   │    대기    │ GRASSUSDT  │ +15.50%  │ +25.3% │    급등    │  ← 클릭 전
│  ☐   │ ⏳ 분석중.. │ BTCUSDT    │  +5.20%  │ +10.1% │  지속상승  │  ← 분석 중
│  ☐   │ ✅ 적합(75)│ ETHUSDT    │  +3.80%  │  +8.5% │  지속상승  │  ← 적합
│  ☐   │ ❌ 부적합(45)│ SOLUSDT  │  -2.10%  │  -5.3% │  지속하락  │  ← 부적합
└──────┴────────────┴────────────┴──────────┴────────┴────────────┘
```

### 3. 상세 결과 다이얼로그

**클릭 시 팝업**:
```
┌────────────────────────────────────────────┐
│  GRASSUSDT 백테스팅 결과 (1주)              │
├────────────────────────────────────────────┤
│  종합 평가: ✅ 적합 (75점)                  │
│  백테스트 기간: 2025-11-13 ~ 2025-11-20    │
├────────────────────────────────────────────┤
│  📊 성능 메트릭                             │
│  ├─ 총 수익률: +5.2% (+520 USDT)           │
│  ├─ 승률: 60.0% (6승 4패)                  │
│  ├─ 총 거래: 10회                          │
│  ├─ 평균 수익: +87 USDT                    │
│  ├─ 평균 손실: -45 USDT                    │
│  ├─ 손익비: 1.93                           │
│  ├─ 최대 낙폭: 3.5%                        │
│  └─ 샤프 비율: 1.2                         │
├────────────────────────────────────────────┤
│  ✅ 적합 판단 근거                          │
│  - 승률 50% 이상 달성 ✓                    │
│  - 수익률 +2% 이상 ✓                       │
│  - 최대 낙폭 5% 이하 ✓                     │
│  - 충분한 거래 샘플 (10회) ✓               │
├────────────────────────────────────────────┤
│  [1주 백테스트]  [1달 백테스트]  [닫기]    │
└────────────────────────────────────────────┘
```

---

## 🎯 적합성 판단 기준 (제안)

### 1. 기본 기준

| 항목 | 적합 | 주의 필요 | 부적합 |
|------|------|----------|--------|
| **승률** | ≥ 50% | 40~50% | < 40% |
| **수익률** | ≥ +2% | 0~2% | < 0% |
| **최대 낙폭** | ≤ 5% | 5~10% | > 10% |
| **거래 횟수** | ≥ 5회 | 3~5회 | < 3회 |
| **샤프 비율** | ≥ 1.0 | 0.5~1.0 | < 0.5 |

### 2. 점수 계산

```python
총점 = 승률 점수(30) + 수익률 점수(40) + MDD 점수(20) + 거래 횟수(10)

점수 → 적합성 매핑:
- 70점 이상 + 승률 50% 이상 + 수익률 +2% 이상 → "적합"
- 50~70점 → "주의 필요"
- 50점 미만 → "부적합"
```

### 3. 추가 고려사항

#### A. 변동성 조정
- **고변동성 코인** (신규상장 등): 승률 기준 완화 (40% 이상)
- **저변동성 코인**: 수익률 기준 강화 (+3% 이상)

#### B. 기간별 가중치
```python
# 1주 백테스트: 최근 트렌드 중심
weight_1w = {"win_rate": 0.3, "pnl": 0.5, "mdd": 0.2}

# 1달 백테스트: 안정성 중심
weight_1m = {"win_rate": 0.4, "pnl": 0.3, "mdd": 0.3}
```

---

## ⚙️ 구현 단계

### Phase 1: 백엔드 API 구현 (1~2일)

1. ✅ **BacktestAdapter 확인** (이미 완료)
2. ⬜ **API 엔드포인트 추가**
   - `/api/v1/backtest/suitability`
   - 파라미터: symbol, period (1w/1m)
3. ⬜ **적합성 판단 로직 구현**
   - `evaluate_suitability()` 함수
   - 점수 계산 알고리즘
4. ⬜ **캐싱 구현** (선택)
   - 동일 심볼 재요청 시 캐시 반환
   - Redis 또는 메모리 캐시

### Phase 2: GUI 통합 (1~2일)

1. ⬜ **RankingTableWidget 수정**
   - 컬럼 명칭 변경: "랭크" → "거래 적합성"
   - 백테스트 상태 표시 로직
   - Signal 추가: `backtest_requested`, `backtest_detail_requested`
2. ⬜ **main.py 연결**
   - 백테스트 요청 핸들러
   - 결과 업데이트 핸들러
3. ⬜ **상세 다이얼로그 구현**
   - 백테스트 결과 팝업
   - 메트릭 시각화

### Phase 3: 테스트 & 최적화 (1일)

1. ⬜ **단위 테스트**
   - 백테스트 API 응답 검증
   - 적합성 판단 로직 검증
2. ⬜ **성능 최적화**
   - 백테스트 속도 개선 (병렬 처리)
   - 데이터 로드 캐싱
3. ⬜ **사용자 테스트**
   - 실제 코인으로 검증
   - UI/UX 피드백 반영

---

## 🚧 구현 시 주의사항

### 1. 백테스트 시간 소요
- **1주 백테스트**: 약 10,080개 1분봉 (7일 × 24시간 × 60분)
- **예상 소요 시간**: 5~15초
- **해결책**: 
  - 비동기 처리 (백그라운드 스레드)
  - 프로그레스 표시 ("⏳ 분석중...")
  - 캐싱 (동일 심볼 재요청 시)

### 2. API 속도 제한
- Binance API: 1200 requests/min
- **해결책**:
  - 데이터 로드 시 배치 처리
  - 캐시 활용 (Redis/메모리)
  - 우선순위 큐 (여러 심볼 동시 요청 시)

### 3. 메모리 사용량
- 1주 백테스트: 약 10,000 캔들 × 3개 타임프레임 = 30,000 rows
- **해결책**:
  - DataFrame 최적화 (필요한 컬럼만 유지)
  - 백테스트 완료 후 데이터 정리
  - 동시 백테스트 개수 제한 (최대 3~5개)

### 4. 신뢰도 문제
- **샘플 부족**: 거래 횟수 < 3회 → "샘플 부족" 경고
- **데이터 부족**: 상장 3일 미만 코인 → "데이터 부족" 표시
- **변동성 극심**: MDD > 20% → "고위험" 경고

---

## 📝 최종 검증 결과

### ✅ 구현 가능성: **100% 가능**

#### 근거
1. ✅ **BacktestAdapter 완성**: 모든 백테스트 로직 구현 완료
2. ✅ **Alpha 전략 통합**: 동일한 Orchestrator 사용 가능
3. ✅ **데이터 로드**: Binance API 연동 완료
4. ✅ **성능 메트릭**: 승률, 수익률, MDD, Sharpe 등 모든 지표 계산 가능
5. ✅ **GUI 구조**: 랭킹 테이블에 쉽게 통합 가능
6. ✅ **기존 분석 플로우**: 유사한 패턴 (`analyze_requested`) 이미 존재

### 📊 예상 효과

1. **사용자 가치**
   - 클릭 한 번으로 거래 적합성 즉시 확인
   - 데이터 기반 의사결정 지원
   - 고위험 코인 필터링

2. **시스템 가치**
   - 백테스트 인프라 활용도 증가
   - 실시간 랭킹과 백테스트 통합
   - 전략 신뢰도 향상

### ⏱️ 예상 구현 기간

- **최소 (MVP)**: 2~3일
  - 백엔드 API 구현: 1일
  - GUI 통합: 1일
  - 테스트: 0.5일

- **완전 구현**: 4~5일
  - 상세 다이얼로그: +1일
  - 캐싱 & 최적화: +1일
  - 사용자 테스트: +0.5일

---

## 🎯 다음 단계 제안

### 1. MVP 구현 우선
```
[ ] 백엔드 API 구현 (/api/v1/backtest/suitability)
[ ] 컬럼 명칭 변경 (랭크 → 거래 적합성)
[ ] 간단한 적합/부적합 표시
[ ] 1주 백테스트만 지원
```

### 2. 점진적 개선
```
[ ] 상세 다이얼로그 추가
[ ] 1달 백테스트 옵션
[ ] 캐싱 구현
[ ] 병렬 처리 최적화
```

### 3. 고급 기능 (선택)
```
[ ] 백테스트 결과 히스토리 저장
[ ] 코인별 비교 차트
[ ] 커스텀 백테스트 기간 설정
[ ] 전략 파라미터 조정 시뮬레이션
```

---

## ✅ 최종 결론

**사용자 요구사항 구현 가능성: 100% 가능**

- ✅ 기술적 준비: BacktestAdapter 완성
- ✅ GUI 통합: 기존 패턴 활용 가능
- ✅ 데이터 소스: Binance API 연동 완료
- ✅ 성능 메트릭: 모든 지표 계산 가능
- ✅ 사용자 경험: 직관적인 클릭 플로우

**즉시 구현 가능하며, MVP는 2~3일 내 완성 가능합니다!** 🚀
