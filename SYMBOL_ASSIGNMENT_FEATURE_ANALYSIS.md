# 🎯 코인 심볼 지정 배치 기능 분석 보고서 (최종 수정)

## 📅 분석 일시
2025-11-13 (최종 업데이트)

---

## ⚠️ 오해 사항 정정

### **1. [설정 적용] 버튼 기능 (정정됨)**

#### **❌ 이전 오해:**
- 바이낸스 거래 페이지만 열기

#### **✅ 올바른 이해:**
- 사용자가 각 엔진에 설정한 **"투입 자금"**과 **"레버리지"**를 
- **바이낸스 API를 통해** 해당 코인 심볼의 거래 설정에 **직접 적용**

**예시:**
```
사용자 설정:
- 코인: BTCUSDT
- 투입 자금: 5,000 USDT
- 레버리지: 10x

[설정 적용] 버튼 클릭 시:
→ 바이낸스 API 호출
→ BTCUSDT 심볼의 레버리지를 10x로 설정
→ (향후) 5,000 USDT로 포지션 진입 준비
```

---

### **2. 2열 버튼 배치 (정정됨)**

#### **❌ 이전 배치:**
```
Coin Momentum & Chart
  [알파] [베타] [감마]
```

#### **✅ 올바른 배치:**
```
----------------------------------------------------------------------------------------------------
'Coin Momentum & Chart' - 'BTCUSDT'        Assign Trading Symbol: [알파] [베타] [감마]
----------------------------------------------------------------------------------------------------
```

**레이아웃 구조:**
```
┌──────────────────────────────────────────────────────────────────────────────┐
│ Coin Momentum & Chart - BTCUSDT    Assign Trading Symbol: [알파][베타][감마] │
├──────────────────────────────────────────────────────────────────────────────┤
│                         (추세 분석 위젯)                                      │
│                         (타이밍 차트)                                         │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 사용자 요구사항 이해 (최종)

## 🎯 사용자 요구사항 이해 (최종)

### **전체 워크플로우**

```
┌─────────────────────────────────────────────────────────────────────┐
│ Step 1: 코인 검색 및 분석                                          │
├─────────────────────────────────────────────────────────────────────┤
│ 1. 실시간 랭킹리스트에서 코인 확인 (100위권)                       │
│ 2. "상승률%", "누적", "상승유형" 행 클릭                           │
│ 3. 2열 "Coin Momentum & Chart"에 추세/차트 표시                    │
│    → 사용자 판단: "이 코인으로 거래하겠다!"                         │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 2: 엔진에 코인 배치                                           │
├─────────────────────────────────────────────────────────────────────┤
│ 1. 2열 상단 "Assign Trading Symbol: [알파][베타][감마]" 확인       │
│ 2. [알파] 버튼 클릭                                                 │
│ 3. 하단 "알파 엔진"의 "Selected Symbol"이 해당 코인으로 변경       │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 3: 거래 설정                                                   │
├─────────────────────────────────────────────────────────────────────┤
│ 1. 알파 엔진에서 "투입 자금" 슬라이더 조절 (예: 50% → 5,000 USDT)  │
│ 2. "레버리지" 슬라이더 조절 (예: 10x)                              │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Step 4: 바이낸스 설정 적용                                          │
├─────────────────────────────────────────────────────────────────────┤
│ 1. [설정 적용] 버튼 클릭                                            │
│ 2. 바이낸스 API 호출:                                               │
│    - POST /fapi/v1/leverage                                         │
│      → BTCUSDT의 레버리지를 10x로 설정                             │
│    - (선택) 포지션 진입 준비 완료 메시지 표시                       │
│ 3. 성공 메시지: "✅ BTCUSDT 설정 적용 완료 (10x, 5000 USDT)"       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔍 현재 시스템 분석

### **1. 바이낸스 API 클라이언트 존재 확인**

#### **파일:** `backend/api_client/binance_client.py`

**현재 상태:**
```python
class BinanceClient:
    def __init__(self, api_key, secret_key, base_url):
        self.api_key = api_key or BINANCE_API_KEY  # .env에서 로드
        self.secret_key = secret_key or BINANCE_SECRET_KEY
        
    def _send_signed_request(self, method, path, params):
        """서명된 API 요청 (레버리지 설정 등)"""
        # HMAC SHA256 서명
        # API 호출
        
    # 기존 구현된 함수들:
    def get_account_info(self)  # 계좌 정보
    def get_klines(self)        # 캔들 데이터
    def get_24hr_ticker(self)   # 24시간 티커
```

**✅ 확인:**
- API 클라이언트 이미 구현됨
- 서명된 요청 (`_send_signed_request`) 지원
- API 키/시크릿 `.env`에서 로드

---

### **2. 레버리지 설정 API 추가 필요**

#### **바이낸스 선물 API 문서:**

**엔드포인트:**
```
POST /fapi/v1/leverage
```

**필수 파라미터:**
```python
{
    "symbol": "BTCUSDT",      # 코인 심볼
    "leverage": 10,           # 레버리지 (1~125x)
    "timestamp": 1699876543210,
    "signature": "..."
}
```

**응답 예시:**
```json
{
    "leverage": 10,
    "maxNotionalValue": "1000000",
    "symbol": "BTCUSDT"
}
```

---

### **3. 필요한 새로운 함수**

#### **파일:** `backend/api_client/binance_client.py`

```python
def set_leverage(self, symbol: str, leverage: int) -> Dict[str, Any]:
    """레버리지 설정
    
    Args:
        symbol: 코인 심볼 (예: "BTCUSDT")
        leverage: 레버리지 배수 (1~125)
        
    Returns:
        {
            "leverage": 10,
            "maxNotionalValue": "1000000",
            "symbol": "BTCUSDT"
        }
        또는 에러 시:
        {
            "error": "에러 메시지",
            "code": -4028
        }
    """
    if not 1 <= leverage <= 125:
        return {"error": "레버리지는 1~125 사이여야 합니다.", "code": -1}
    
    params = {
        "symbol": symbol,
        "leverage": leverage
    }
    
    # Weight = 1
    return self._send_signed_request(
        "POST", 
        "/fapi/v1/leverage", 
        params=params,
        weight_category="general",
        weight=1
    )
```

---

## ✅ 구현 가능성 분석 (최종)

### **1. 바이낸스 API 레버리지 설정**

| 항목 | 상태 | 비고 |
|------|------|------|
| **API 클라이언트 존재** | ✅ 완료 | `BinanceClient` 이미 구현됨 |
| **서명된 요청 지원** | ✅ 완료 | `_send_signed_request()` 존재 |
| **API 키/시크릿** | ✅ 설정됨 | `.env` 파일에서 로드 |
| **레버리지 설정 API** | ❌ 추가 필요 | `set_leverage()` 함수 구현 |

**난이도:** ⭐⭐ (쉬움)  
**소요 시간:** 15분  
**구현 가능:** ✅ **100% 가능**

---

### **2. 2열 버튼 배치**

**레이아웃 코드:**
```python
# gui/main.py Line 180
entry_header = QHBoxLayout()

# 제목 + 현재 심볼
title_symbol = QLabel(f"Coin Momentum & Chart - {self.selected_symbol or '-'}")
entry_header.addWidget(title_symbol)

entry_header.addStretch()

# "Assign Trading Symbol:" 라벨
assign_label = QLabel("Assign Trading Symbol:")
assign_label.setStyleSheet("color: #666666; font-size: 10px;")
entry_header.addWidget(assign_label)

# [알파] [베타] [감마] 버튼
self.alpha_btn = QPushButton("[알파]")
self.alpha_btn.setStyleSheet("background-color: #4CAF50; color: white; ...")
self.alpha_btn.clicked.connect(lambda: self._assign_to_engine("Alpha"))
entry_header.addWidget(self.alpha_btn)

self.beta_btn = QPushButton("[베타]")
self.beta_btn.setStyleSheet("background-color: #2196F3; color: white; ...")
self.beta_btn.clicked.connect(lambda: self._assign_to_engine("Beta"))
entry_header.addWidget(self.beta_btn)

self.gamma_btn = QPushButton("[감마]")
self.gamma_btn.setStyleSheet("background-color: #FF9800; color: white; ...")
self.gamma_btn.clicked.connect(lambda: self._assign_to_engine("Gamma"))
entry_header.addWidget(self.gamma_btn)
```

**난이도:** ⭐ (매우 쉬움)  
**소요 시간:** 10분  
**구현 가능:** ✅ **100% 가능**

---

### **3. [설정 적용] 버튼 기능**

#### **엔진 위젯 함수:**
```python
# gui/widgets/footer_engines_widget.py

def _on_apply_settings(self):
    """설정 적용 버튼 - 바이낸스 API로 레버리지 설정"""
    if not self.selected_symbol:
        self._add_trade_message("❌ 먼저 코인을 배치하세요!")
        return
    
    # 현재 설정 읽기
    funds = self.designated_funds
    leverage = self.leverage_slider.value()
    
    # 바이낸스 API 호출
    from backend.api_client.binance_client import BinanceClient
    
    client = BinanceClient()
    
    # 1. 레버리지 설정
    result = client.set_leverage(self.selected_symbol, leverage)
    
    if "error" in result:
        self._add_trade_message(
            f"❌ 설정 실패: {result.get('error')}"
        )
        return
    
    # 2. 성공 메시지
    self._add_trade_message(
        f"✅ {self.selected_symbol} 설정 적용 완료\n"
        f"   레버리지: {leverage}x\n"
        f"   투입 자금: {funds:.2f} USDT"
    )
    
    # 3. 시그널 발송 (향후 자동 거래 연동)
    self.settings_changed.emit(self.engine_name, {
        "symbol": self.selected_symbol,
        "funds": funds,
        "leverage": leverage
    })
```

**난이도:** ⭐⭐⭐ (중간)  
**소요 시간:** 20분  
**구현 가능:** ✅ **100% 가능**

**주의사항:**
- API 키 권한 필요: "Futures Trading" 활성화
- 테스트넷에서 먼저 테스트 권장

---

### **4. 투입 자금 슬라이더 변경**

**현재 (텍스트 입력):**
```python
self.funds_input = QLineEdit()
self.funds_input.setText("0")
```

**변경 후 (슬라이더):**
```python
# 총 자금 (향후 헤더에서 설정 가능)
self.total_funds = 10000.0  # 기본 10,000 USDT

# 슬라이더 (0% ~ 100%)
self.funds_slider = QSlider(Qt.Horizontal)
self.funds_slider.setMinimum(0)
self.funds_slider.setMaximum(100)
self.funds_slider.setValue(0)
self.funds_slider.valueChanged.connect(self._on_funds_changed)

# 비율 + 금액 표시
self.funds_label = QLabel("0% (0.00 USDT)")

def _on_funds_changed(self, percentage: int):
    """투입 자금 슬라이더 변경"""
    amount = self.total_funds * (percentage / 100.0)
    self.designated_funds = amount
    self.funds_label.setText(f"{percentage}% ({amount:.2f} USDT)")
```

**난이도:** ⭐⭐ (쉬움)  
**소요 시간:** 15분  
**구현 가능:** ✅ **100% 가능**

---

## 📊 최종 구현 계획

### **Phase 1: 백엔드 API 추가 (15분)**

#### **파일:** `backend/api_client/binance_client.py`
```python
def set_leverage(self, symbol: str, leverage: int) -> Dict[str, Any]:
    """레버리지 설정 (바이낸스 선물 API)"""
    # 구현 내용 위에 작성됨
```

---

### **Phase 2: GUI 레이아웃 변경 (25분)**

#### **파일 1:** `gui/main.py`
- ✅ 2열 헤더 레이아웃 변경
- ✅ `_assign_to_engine(engine_name)` 함수 추가

#### **파일 2:** `gui/widgets/footer_engines_widget.py`
- ✅ "심볼 지정" → "설정 적용" 버튼 교체
- ✅ 투입 자금 텍스트 → 슬라이더 변경
- ✅ `_on_apply_settings()` 함수 추가
- ✅ `set_symbol(symbol)` 함수 추가

---

### **Phase 3: 테스트 및 검증 (10분)**

1. ✅ 랭킹 테이블 클릭 → selected_symbol 업데이트
2. ✅ [알파] 버튼 클릭 → 알파 엔진에 심볼 배치
3. ✅ 투입 자금 슬라이더 조절 → 금액 표시
4. ✅ 레버리지 슬라이더 조절 → 값 표시
5. ✅ [설정 적용] 클릭 → 바이낸스 API 호출
6. ✅ 성공 메시지 확인

---

## ⚠️ 중요 주의사항

### **1. API 키 권한 설정**

**바이낸스 계정 → API Management:**
```
✅ Enable Futures (선물 거래 활성화)
✅ Enable Reading (계좌 정보 읽기)
❌ Enable Withdrawals (출금 비활성화 권장)
```

**보안:**
- API 키는 절대 공유 금지
- `.env` 파일은 `.gitignore`에 추가

---

### **2. 테스트넷 사용 권장**

**테스트넷 URL:**
```
https://testnet.binancefuture.com
```

**.env 설정:**
```bash
BINANCE_BASE_URL=https://testnet.binancefuture.com
BINANCE_API_KEY=테스트넷_API_키
BINANCE_SECRET_KEY=테스트넷_시크릿_키
```

---

### **3. 에러 처리**

**가능한 에러:**
```python
# 1. API 키 없음
{"error": "API 키가 설정되지 않았습니다.", "code": -1}

# 2. 권한 부족
{"error": "API-key format invalid.", "code": -2014}

# 3. 레버리지 범위 초과
{"error": "Leverage is over the maximum defined for this symbol.", "code": -4028}

# 4. 심볼 없음
{"error": "Invalid symbol.", "code": -1121}
```

**처리 방법:**
```python
result = client.set_leverage(symbol, leverage)

if "error" in result:
    error_msg = result.get("error", "알 수 없는 오류")
    error_code = result.get("code", -1)
    
    # 사용자 친화적 메시지
    if error_code == -2014:
        msg = "API 키 권한이 부족합니다. Futures 권한을 활성화하세요."
    elif error_code == -4028:
        msg = f"레버리지 {leverage}x는 이 코인에서 사용할 수 없습니다."
    else:
        msg = f"설정 실패: {error_msg}"
    
    self._add_trade_message(f"❌ {msg}")
```

---

## ✅ 최종 결론

### **구현 가능성: 100% ✅**

| 기능 | 난이도 | 소요시간 | 구현 가능 |
|------|--------|----------|-----------|
| **바이낸스 레버리지 설정 API** | ⭐⭐ 쉬움 | 15분 | ✅ 완전 가능 |
| **2열 버튼 배치** | ⭐ 매우 쉬움 | 10분 | ✅ 완전 가능 |
| **[설정 적용] 버튼 기능** | ⭐⭐⭐ 중간 | 20분 | ✅ 완전 가능 |
| **투입 자금 슬라이더** | ⭐⭐ 쉬움 | 15분 | ✅ 완전 가능 |
| **엔진 심볼 배치 연동** | ⭐⭐ 쉬움 | 10분 | ✅ 완전 가능 |

**총 소요 시간:** 약 **70분** (1시간 10분)

---

### **구현 방법:**

1. ✅ **바이낸스 API 클라이언트에 `set_leverage()` 추가**
   - 기존 `_send_signed_request()` 활용
   - 서명/인증 자동 처리
   
2. ✅ **GUI 레이아웃 변경**
   - 2열 헤더: 제목 + 심볼 + "Assign Trading Symbol:" + 버튼 3개
   - 엔진 위젯: 슬라이더 2개 + "설정 적용" 버튼
   
3. ✅ **이벤트 연동**
   - 랭킹 클릭 → `selected_symbol` 저장
   - [알파/베타/감마] 클릭 → 엔진에 심볼 전달
   - [설정 적용] 클릭 → API 호출 + 메시지 표시

---

### **기술적 제약 없음:**

- ✅ 바이낸스 API 공식 지원
- ✅ API 클라이언트 이미 구현됨
- ✅ 서명된 요청 지원
- ✅ Qt 위젯 시스템 완비
- ✅ 시그널/슬롯 연동 간단

---

## 📝 사용자 확인 필요 사항

1. **총 자금 설정 방법?**
   - 옵션 A: 헤더에 "총 자금" 입력 필드 추가
   - 옵션 B: 각 엔진마다 개별 설정
   - **권장:** 헤더에 전역 설정 (3개 엔진이 비율로 나눠 사용)

2. **API 키 설정?**
   - `.env` 파일에 이미 설정되어 있나요?
   - 테스트넷 사용할까요? (실거래 전 권장)

3. **버튼 크기/스타일?**
   - 현재 제안: 작은 버튼 (70px × 24px)
   - 다른 크기 원하시나요?

---

**보고서 최종 업데이트:** 2025-11-13  
**분석자:** GitHub Copilot  
**결론:** ✅ **사용자 요구사항 완전 이해, 100% 구현 가능**  
**사용자 명령 대기 중** 🎯

#### **Step 1: 코인 검색 (중단 세션 - 실시간 랭킹리스트)**
```
사용자 행동:
1. 실시간 랭킹리스트에서 100위권 코인 확인
2. 관심 코인의 "상승률%", "누적", "상승유형" 행 클릭
   ↓
3. 2열 "Coin Momentum & Chart"에 해당 코인 표시
   - 추세 분석 위젯 표시
   - 타이밍 분석 차트 표시
   ↓
4. 사용자 판단: "이 코인으로 거래하겠다!"
```

**현재 코드 확인:**
- ✅ `ranking_table` 클릭 → `selected_symbol` 업데이트 (Line 336)
- ✅ 2열에 추세/차트 자동 표시 (확인됨)

---

### **2. 요청 기능: 엔진 지정 배치**

#### **기능 위치**
```
[Coin Momentum & Chart] 섹션 (2열 상단)
  ├─ 현재: "Coin Momentum & Chart" 제목만 존재
  └─ 추가: [알파] [베타] [감마] 버튼 3개 나란히 배치
```

#### **사용자 시나리오**
```
Step 1: 랭킹 테이블에서 코인 클릭 (예: BTCUSDT)
  ↓
Step 2: 2열에서 해당 코인 추세/차트 확인
  ↓
Step 3: "[알파]" 버튼 클릭
  ↓
Step 4: 하단 "알파 엔진"의 "Selected Symbol"이 "BTCUSDT"로 변경됨
  ↓
Step 5: 사용자가 "투입 자금" 및 "레버리지" 설정
  ↓
Step 6: "[설정 적용]" 버튼 클릭
  ↓
Step 7: 바이낸스 선물 거래 페이지에 설정 적용
```

---

## 🔍 현재 코드 구조 분석

### **1. 메인 윈도우 (`gui/main.py`)**

#### **현재 상태:**
```python
# Line 180-183: 2열 상단
self.entry_title = QLabel("Coin Momentum & Chart")
entry_header.addWidget(self.entry_title)
entry_header.addStretch()
```

#### **현재 심볼 선택 메커니즘:**
```python
# Line 336: 랭킹 테이블 클릭 시
self.selected_symbol = symbol  # 메인 윈도우에 저장됨
```

---

### **2. 엔진 위젯 (`gui/widgets/footer_engines_widget.py`)**

#### **현재 심볼 지정 방식 (Line 466-482):**
```python
def _on_symbol_select(self):
    """심볼 지정 버튼 클릭 - 간단한 다이얼로그"""
    symbols = ["BTCUSDT", "ETHUSDT", ...]  # 고정 리스트
    
    symbol, ok = QInputDialog.getItem(
        self, "심볼 선택", "거래할 코인 심볼을 선택하세요:", symbols
    )
    
    if ok and symbol:
        self.selected_symbol = symbol
        self.symbol_label.setText(symbol)
```

**문제점:**
- ❌ 사용자가 랭킹에서 선택한 코인과 **연동 안 됨**
- ❌ 고정된 15개 리스트에서만 선택 가능
- ❌ 랭킹에서 확인한 코인을 **수동으로 다시 입력해야 함**

---

#### **현재 설정 구조 (Line 193-257):**
```python
# Designated Funds (투입 자금) - 텍스트 입력
self.funds_input = QLineEdit()
self.funds_input.setText("0")
self.funds_input.setValidator(QDoubleValidator(0.0, 999999.0, 2))

# Applied Leverage (레버리지) - 슬라이더
self.leverage_slider = QSlider(Qt.Horizontal)
self.leverage_slider.setMinimum(1)
self.leverage_slider.setMaximum(50)
self.leverage_slider.setValue(1)
```

**확인:**
- ✅ 투입 자금: 텍스트 입력 방식 (0~999,999 USDT)
- ✅ 레버리지: 슬라이더 방식 (1x~50x)

---

#### **현재 버튼 구조 (Line 135-148):**
```python
# 심볼 지정 버튼
self.symbol_select_button = QPushButton("심볼 지정")
self.symbol_select_button.clicked.connect(self._on_symbol_select)

# 거래 활성화 토글 버튼
self.toggle_button = QPushButton("거래 활성화")
self.toggle_button.setCheckable(True)
self.toggle_button.clicked.connect(self._on_toggle_clicked)
```

---

## ✅ 요구사항 정리

### **변경사항 1: 2열에 엔진 배치 버튼 추가**

#### **위치:**
`gui/main.py` Line 180-183

#### **현재:**
```python
entry_header.addWidget(self.entry_title)
entry_header.addStretch()
```

#### **변경 후:**
```python
entry_header.addWidget(self.entry_title)
entry_header.addStretch()

# [알파] [베타] [감마] 버튼 추가
self.alpha_assign_btn = QPushButton("[알파]")
self.beta_assign_btn = QPushButton("[베타]")
self.gamma_assign_btn = QPushButton("[감마]")

# 스타일링
self.alpha_assign_btn.setStyleSheet("background-color: #4CAF50; ...")  # 녹색
self.beta_assign_btn.setStyleSheet("background-color: #2196F3; ...")   # 파랑
self.gamma_assign_btn.setStyleSheet("background-color: #FF9800; ...")  # 주황

# 클릭 이벤트 연결
self.alpha_assign_btn.clicked.connect(lambda: self._assign_symbol_to_engine("Alpha"))
self.beta_assign_btn.clicked.connect(lambda: self._assign_symbol_to_engine("Beta"))
self.gamma_assign_btn.clicked.connect(lambda: self._assign_symbol_to_engine("Gamma"))

entry_header.addWidget(self.alpha_assign_btn)
entry_header.addWidget(self.beta_assign_btn)
entry_header.addWidget(self.gamma_assign_btn)
```

#### **새로운 함수:**
```python
def _assign_symbol_to_engine(self, engine_name: str):
    """선택된 심볼을 특정 엔진에 배치"""
    if not self.selected_symbol:
        # 경고 메시지: "먼저 랭킹 테이블에서 코인을 선택하세요"
        return
    
    # 하단 푸터의 해당 엔진에 심볼 전달
    if engine_name == "Alpha":
        self.footer_widget.alpha_engine.set_symbol(self.selected_symbol)
    elif engine_name == "Beta":
        self.footer_widget.beta_engine.set_symbol(self.selected_symbol)
    elif engine_name == "Gamma":
        self.footer_widget.gamma_engine.set_symbol(self.selected_symbol)
```

---

### **변경사항 2: 엔진 위젯의 "심볼 지정" 버튼 삭제**

#### **위치:**
`gui/widgets/footer_engines_widget.py` Line 135-148

#### **현재:**
```python
# 심볼 지정 버튼
self.symbol_select_button = QPushButton("심볼 지정")
self.symbol_select_button.clicked.connect(self._on_symbol_select)
row1_layout.addWidget(self.symbol_select_button)
```

#### **변경 후:**
```python
# [심볼 지정] 버튼 삭제
# 대신 [설정 적용] 버튼 추가
self.apply_settings_button = QPushButton("설정 적용")
self.apply_settings_button.clicked.connect(self._on_apply_settings)
row1_layout.addWidget(self.apply_settings_button)
```

---

### **변경사항 3: [설정 적용] 버튼 기능 구현**

#### **위치:**
`gui/widgets/footer_engines_widget.py`

#### **새로운 함수:**
```python
def _on_apply_settings(self):
    """설정 적용 버튼 클릭 - 바이낸스 거래 페이지에 설정 적용"""
    import webbrowser
    
    if not self.selected_symbol:
        # 경고: "먼저 코인을 배치하세요"
        return
    
    # 현재 설정 읽기
    funds = float(self.funds_input.text() or "0")
    leverage = self.leverage_slider.value()
    
    # 바이낸스 선물 거래 페이지 열기
    url = f"https://www.binance.com/en/futures/{self.selected_symbol}"
    webbrowser.open(url)
    
    # 메시지 표시
    self._add_trade_message(
        f"✅ 설정 적용: {self.selected_symbol} | "
        f"자금: {funds} USDT | 레버리지: {leverage}x"
    )
    
    # 시그널 발송 (추후 자동 거래 연동 시 사용)
    self.settings_changed.emit(self.engine_name, {
        "symbol": self.selected_symbol,
        "funds": funds,
        "leverage": leverage
    })
```

**참고:**
- 바이낸스 API는 레버리지/자금 설정을 직접 변경할 수 없음 (보안상 제한)
- 현재는 **거래 페이지를 열어주는 방식**으로 사용자가 수동 설정
- 향후 API 키 연동 시 자동화 가능

---

### **변경사항 4: 투입 자금을 슬라이더 바로 변경**

#### **현재 (텍스트 입력):**
```python
self.funds_input = QLineEdit()
self.funds_input.setText("0")
self.funds_input.setValidator(QDoubleValidator(0.0, 999999.0, 2))
```

#### **변경 후 (슬라이더 + 비율 표시):**
```python
# 사용자 총 자금 설정 (예: 10,000 USDT)
self.total_funds = 10000.0  # 향후 설정 UI에서 변경 가능

# 슬라이더 (0% ~ 100%)
self.funds_slider = QSlider(Qt.Horizontal)
self.funds_slider.setMinimum(0)
self.funds_slider.setMaximum(100)  # 0%~100%
self.funds_slider.setValue(0)
self.funds_slider.valueChanged.connect(self._on_funds_slider_changed)

# 비율 및 금액 표시
self.funds_label = QLabel("0% (0 USDT)")
self.funds_label.setStyleSheet("color: #ffffff; font-size: 9px; font-weight: bold;")

def _on_funds_slider_changed(self, value: int):
    """투입 자금 슬라이더 변경"""
    percentage = value  # 0~100
    amount = self.total_funds * (percentage / 100.0)
    
    self.designated_funds = amount
    self.funds_label.setText(f"{percentage}% ({amount:.2f} USDT)")
    
    self._on_settings_changed()
```

---

### **변경사항 5: 외부에서 심볼 설정하는 함수 추가**

#### **위치:**
`gui/widgets/footer_engines_widget.py`

#### **새로운 Public 함수:**
```python
def set_symbol(self, symbol: str):
    """외부에서 심볼 배치 (메인 윈도우에서 호출)"""
    if not symbol:
        return
    
    self.selected_symbol = symbol
    self.symbol_label.setText(symbol)
    self.symbol_label.setStyleSheet("color: #4CAF50; font-size: 10px; font-weight: bold;")  # 녹색 강조
    
    # 메시지 추가
    self._add_energy_message(f"🎯 {symbol} 배치 완료!")
    
    # 시그널 발송
    self.symbol_changed.emit(self.engine_name, symbol)
```

---

## 📊 구현 가능성 분석

### **✅ 완전 구현 가능**

#### **1. 2열 버튼 추가**
- **난이도:** ⭐ (매우 쉬움)
- **소요 시간:** 5분
- **기술적 제약:** 없음

#### **2. 엔진 심볼 배치 연동**
- **난이도:** ⭐⭐ (쉬움)
- **소요 시간:** 10분
- **기술적 제약:** 없음
- **방법:** 메인 윈도우의 `selected_symbol` → 엔진 위젯의 `set_symbol()` 호출

#### **3. "심볼 지정" → "설정 적용" 버튼 변경**
- **난이도:** ⭐ (매우 쉬움)
- **소요 시간:** 5분
- **기술적 제약:** 없음

#### **4. 투입 자금 슬라이더 변경**
- **난이도:** ⭐⭐ (쉬움)
- **소요 시간:** 15분
- **기술적 제약:** 없음
- **추가 필요:** 총 자금 설정 UI (향후 헤더에 추가 가능)

#### **5. 바이낸스 설정 적용**
- **난이도:** ⭐⭐⭐ (중간)
- **소요 시간:** 현재는 페이지 열기만 (5분), API 연동 시 (2시간)
- **기술적 제약:**
  - ✅ **페이지 열기**: 즉시 가능 (`webbrowser.open()`)
  - ⚠️ **자동 설정**: 바이낸스 API 키 필요 (사용자 권한 필요)
  - ⚠️ **레버리지 변경**: API 키 + 추가 권한 필요

---

## 🎯 최종 구현 방안

### **Phase 1: 즉시 구현 가능 (30분)**

#### **1단계: UI 변경**
```
✅ 2열에 [알파] [베타] [감마] 버튼 추가
✅ 엔진 위젯의 "심볼 지정" → "설정 적용" 변경
✅ 투입 자금 텍스트 입력 → 슬라이더 변경
```

#### **2단계: 기능 연동**
```
✅ 랭킹 클릭 → selected_symbol 저장
✅ [알파/베타/감마] 클릭 → 해당 엔진에 심볼 배치
✅ 엔진 위젯에 심볼 표시 ("Selected Symbol: BTCUSDT")
```

#### **3단계: 설정 적용**
```
✅ [설정 적용] 클릭 → 바이낸스 거래 페이지 열기
✅ 메시지 표시: "설정 적용: BTCUSDT | 자금: 5000 USDT | 레버리지: 10x"
```

---

### **Phase 2: 향후 확장 (2시간, API 키 필요)**

#### **자동 설정 적용 (선택 사항)**
```python
# 바이낸스 API 연동
import ccxt

exchange = ccxt.binance({
    'apiKey': 'YOUR_API_KEY',
    'secret': 'YOUR_SECRET',
    'options': {'defaultType': 'future'}
})

# 레버리지 설정
exchange.set_leverage(leverage, symbol)

# 주문 실행 (향후)
exchange.create_market_buy_order(symbol, amount)
```

**주의:**
- API 키는 **사용자 보안 정보**이므로 신중하게 처리
- 자동 거래는 **사용자 명시적 동의** 필요

---

## 📝 코드 변경 위치 요약

### **파일 1: `gui/main.py`**

#### **변경 위치:**
- Line 180-183: 2열 헤더에 버튼 3개 추가
- 새로운 함수 추가: `_assign_symbol_to_engine(engine_name)`

---

### **파일 2: `gui/widgets/footer_engines_widget.py`**

#### **변경 위치:**
- Line 135-148: "심볼 지정" 버튼 → "설정 적용" 버튼 변경
- Line 193-212: 투입 자금 텍스트 입력 → 슬라이더 변경
- Line 466-482: `_on_symbol_select()` 삭제
- 새로운 함수 추가:
  - `set_symbol(symbol)`: 외부에서 심볼 배치
  - `_on_apply_settings()`: 설정 적용 버튼 핸들러
  - `_on_funds_slider_changed(value)`: 투입 자금 슬라이더 핸들러

---

## ✅ 검증 완료

### **사용자 의도 이해:**
1. ✅ 랭킹 테이블에서 코인 검색 → 2열에서 추세 확인
2. ✅ 2열 버튼으로 엔진에 즉시 배치 (클릭 1번)
3. ✅ 엔진에서 자금/레버리지 설정 → 바이낸스 적용
4. ✅ 투입 자금은 슬라이더로 비율 조절

### **구현 가능성:**
- ✅ **즉시 구현 가능** (30분, 기술적 제약 없음)
- ✅ **완벽한 UX 개선** (클릭 3번 → 클릭 2번)
- ✅ **바이낸스 연동** (페이지 열기는 즉시, API는 향후)

---

## 🚀 다음 단계

**사용자 확인 필요 사항:**
1. ✅ 투입 자금 슬라이더의 "총 자금" 기준은?
   - 옵션 A: 헤더에서 설정 (예: 10,000 USDT)
   - 옵션 B: 각 엔진마다 별도 설정
   
2. ✅ [설정 적용] 버튼 동작은?
   - 현재 방안: 바이낸스 페이지 열기 (즉시 가능)
   - 향후 방안: API 자동 설정 (API 키 필요)

3. ✅ 버튼 색상/스타일 확인
   - [알파]: 녹색 `#4CAF50`
   - [베타]: 파랑 `#2196F3`
   - [감마]: 주황 `#FF9800`

---

**보고서 작성 완료**  
**작성 일시:** 2025-11-13  
**분석자:** GitHub Copilot  
**결론:** ✅ **완전 구현 가능, 사용자 명령 대기 중**
