# 전략 진입/청산 조건 현황 보고서

**생성일**: 2025-11-20  
**대상**: Alpha/Beta/Gamma 전략 (공통 구조)

---

## 📊 요약

Alpha, Beta, Gamma 세 엔진은 모두 동일한 `StrategyOrchestrator`를 사용하며, 진입 조건과 목표 수익률 설정이 **완전히 동일**합니다.

### 핵심 파라미터 위치
- **진입 조건**: `backend/core/new_strategy/signal_engine.py` - `SignalEngineConfig`
- **목표 수익률**: `backend/core/new_strategy/risk_manager.py` - `RiskManagerConfig`

---

## 🎯 1. 진입 조건 (SignalEngineConfig)

### 📍 위치
```python
# backend/core/new_strategy/signal_engine.py (lines 11-25)
@dataclass
class SignalEngineConfig:
    min_entry_score: float = 100.0        # 워치리스트 진입 최소 점수
    strong_entry_score: float = 130.0     # 강력 진입 점수
    instant_entry_score: float = 160.0    # 즉시 진입 점수
```

### 현재 설정

| 구분 | 점수 | 의미 | 동작 |
|------|------|------|------|
| **워치리스트** | 100점 | 관심 구간 진입 | WATCHLIST 이벤트 발생 (진입 대기) |
| **강력 진입** | 130점 | 강한 상승 신호 | **실제 진입 주문 실행** |
| **즉시 진입** | 160점 | 매우 강한 신호 | **즉시 진입 주문 실행** |

### 점수 산출 체계 (최대 170점)

| 조건 | 가중치 | 설명 |
|------|--------|------|
| Volume spike | 30.0 | 거래량 급증 |
| VWAP breakout | 25.0 | 종가 > VWAP 돌파 |
| 5m uptrend | 20.0 | EMA20 > EMA60 (상승 추세) |
| EMA alignment | 20.0 | EMA 5>10>20>60>120 정렬 |
| Consecutive rise | 15.0 | EMA20 상승 기울기 |
| 3m trend confirm | 20.0 | 3분봉 상승 추세 확인 |
| Bear energy fade | 15.0 | MACD 히스토그램 증가 |
| MACD golden cross | 15.0 | MACD 골든 크로스 |
| RSI oversold rebound | 10.0 | RSI 과매도 반등 |
| **총점** | **170.0** | |

### 진입 필터

```python
# 15분봉이 하락 추세면 진입 억제
if filter_15m.trend == "DOWNTREND":
    return HOLD  # 진입 거부
```

---

## 💰 2. 목표 수익률 및 리스크 관리 (RiskManagerConfig)

### 📍 위치
```python
# backend/core/new_strategy/risk_manager.py (lines 19-33)
@dataclass
class RiskManagerConfig:
    stop_loss_pct: float = 0.005           # 0.5% 손절
    tp_primary_pct: float = 0.02           # 2.0% 선확정 익절
    tp_extended_pct: float = 0.035         # 3.5% 확장 익절 목표
    trailing_stop_pct: float = 0.006       # 0.6% 트레일링 스탑
    breakeven_trigger_pct: float = 0.01    # 1.0% 수익 시 본절 이동
    
    # 확장 익절 조건
    extended_energy_score_threshold: float = 130.0  # 점수 130점 이상 필요
```

### 현재 설정 상세

#### A. 손절 (Stop Loss)
- **손절 비율**: `-0.5%` (고정)
- **동작**: 진입가 대비 -0.5% 도달 시 즉시 청산
- **우선순위**: 최우선 (가장 먼저 체크)

#### B. 목표 수익률 (Take Profit)

##### 1차 목표: +2.0% (선확정)
```
진입 → +2% 도달 → 최소 +2% 확정
                 ↓
            상승 에너지 평가
                 ↓
    ┌────────────┴────────────┐
    ↓                          ↓
에너지 충분 (점수 ≥130)    에너지 부족 (점수 <130)
목표를 +3.5%로 확장        즉시 +2% 익절
```

- **조건**: 진입가 대비 +2% 도달
- **동작**: 
  - 스탑을 최소 `진입가 * 1.02`로 상향 (최소 +2% 확정)
  - 상승 에너지(신호 점수) 평가:
    - **점수 ≥ 130**: 목표를 +3.5%로 확장, 계속 보유
    - **점수 < 130**: 즉시 +2%에서 익절

##### 2차 목표: +3.5% (확장 익절)
- **조건**: 
  1. +2% 도달 시 에너지 충분 (점수 ≥130)
  2. 가격이 `진입가 * 1.035`에 도달
- **동작**: +3.5% 도달 시 익절

#### C. 트레일링 스탑

##### 활성화 조건
- **트리거**: +1% 수익 도달 시 자동 활성화
- **활성화 동작**:
  1. 스탑을 진입가(본절)로 이동
  2. `TRAILING_ACTIVATED` 이벤트 발생
  3. 이후 최고가 추적 시작

##### 트레일링 로직
```
최고가 갱신 → 스탑 = 최고가 * (1 - 0.006)
            = 최고가 - 0.6%
```

- **추적 거리**: 최고가 대비 `-0.6%`
- **최소 보장**: +2% 도달 후에는 스탑이 `진입가 * 1.02` 아래로 내려가지 않음
- **청산**: 현재가가 트레일링 스탑 이하로 하락 시

#### D. 본절 이동 (Breakeven)
- **트리거**: +1% 수익 도달
- **동작**: 스탑을 진입가로 이동 (손실 방지)

---

## 📈 3. 수익률 시나리오 예시

### 시나리오 A: 빠른 +2% 도달, 에너지 부족
```
진입: $100
+1% ($101) → 본절 이동, 트레일링 활성화
+2% ($102) → 에너지 점수: 115 (< 130)
           → 즉시 익절 at +2%
```
**결과**: +2% 수익 확정

### 시나리오 B: +2% 도달, 에너지 충분
```
진입: $100
+1% ($101) → 본절 이동, 트레일링 활성화
+2% ($102) → 에너지 점수: 145 (≥ 130)
           → 목표 확장 to +3.5% ($103.5)
           → 스탑: 최소 $102 유지
+3.5% ($103.5) → 익절
```
**결과**: +3.5% 수익 확정

### 시나리오 C: +2% 도달 후 하락 (트레일링 작동)
```
진입: $100
+1% ($101) → 본절 이동, 트레일링 활성화
+2% ($102) → 스탑: $102 (최소 확정)
최고가: $102.80 → 트레일링 스탑: $102.80 * 0.994 = $102.18
가격 하락: $102.10 → 트레일링 청산
```
**결과**: +2.1% 수익 확정

### 시나리오 D: 손절
```
진입: $100
가격 하락: $99.50 (−0.5%)
→ 즉시 손절
```
**결과**: -0.5% 손실

---

## 🔧 4. 설정 변경 가능 항목

### A. 진입 조건 조정

#### 1) 진입 난이도 조절
```python
# backend/core/new_strategy/signal_engine.py

# 현재
min_entry_score: float = 100.0
strong_entry_score: float = 130.0
instant_entry_score: float = 160.0

# 더 쉽게 진입 (낮은 점수 허용)
min_entry_score: float = 80.0
strong_entry_score: float = 110.0
instant_entry_score: float = 140.0

# 더 보수적 진입 (높은 점수 요구)
min_entry_score: float = 120.0
strong_entry_score: float = 150.0
instant_entry_score: float = 170.0
```

#### 2) 조건별 가중치 조절
```python
# 예: 거래량 급증을 더 중요하게
w_volume_spike: float = 40.0  # 30.0 → 40.0

# 예: VWAP 돌파를 덜 중요하게
w_vwap_breakout: float = 15.0  # 25.0 → 15.0
```

### B. 목표 수익률 조정

#### 1) 보수적 설정 (빠른 익절)
```python
# backend/core/new_strategy/risk_manager.py

stop_loss_pct: float = 0.005           # 0.5% 유지
tp_primary_pct: float = 0.015          # 1.5% (2.0% → 1.5%)
tp_extended_pct: float = 0.025         # 2.5% (3.5% → 2.5%)
trailing_stop_pct: float = 0.008       # 0.8% (0.6% → 0.8%)
```

#### 2) 공격적 설정 (큰 수익 추구)
```python
stop_loss_pct: float = 0.007           # 0.7% (0.5% → 0.7%)
tp_primary_pct: float = 0.03           # 3.0% (2.0% → 3.0%)
tp_extended_pct: float = 0.05          # 5.0% (3.5% → 5.0%)
trailing_stop_pct: float = 0.005       # 0.5% (0.6% → 0.5%)
```

#### 3) 상승 에너지 임계치 조정
```python
# 더 쉽게 확장 익절 허용
extended_energy_score_threshold: float = 110.0  # 130.0 → 110.0

# 더 엄격하게 확장 익절 허용
extended_energy_score_threshold: float = 150.0  # 130.0 → 150.0
```

---

## ⚙️ 5. 엔진별 독립 설정 방법

현재는 Alpha/Beta/Gamma 모두 동일한 설정을 사용합니다. 엔진별로 다른 설정을 원할 경우:

### 방법 A: 전략 파일에서 설정 주입

```python
# backend/core/strategies/alpha_strategy.py

from backend.core.new_strategy import SignalEngine, SignalEngineConfig

# Alpha 전용 설정
alpha_signal_config = SignalEngineConfig(
    min_entry_score=80.0,      # 더 공격적
    strong_entry_score=110.0,
    instant_entry_score=140.0,
)

self.orchestrator = StrategyOrchestrator(
    binance_client=self.binance_client,
    signal=SignalEngine(config=alpha_signal_config),  # 커스텀 설정 주입
    config=self.orch_config,
)
```

### 방법 B: OrchestratorConfig 확장

```python
# backend/core/new_strategy/orchestrator.py

@dataclass
class OrchestratorConfig:
    # 기존 필드...
    
    # 추가
    signal_config: Optional[SignalEngineConfig] = None
    risk_config: Optional[RiskManagerConfig] = None

# __init__에서 사용
self.signal = signal or SignalEngine(config.signal_config if config and config.signal_config else None)
```

---

## 📋 6. 청산 조건 (Exit Conditions)

### A. 신호 기반 청산

포지션 진입 후 다음 조건 중 하나라도 만족 시 청산 신호 발생:

| 조건 | 점수 | 설명 |
|------|------|------|
| EMA 역전 | 50점 | EMA20 < EMA60 * 0.999 |
| MACD 데드크로스 | 40점 | MACD Line < Signal |
| 히스토그램 하락 | 20점 | 현재 히스토그램 < 이전 |
| Stoch RSI 과매수 교차 | 20점 | K < D (과매수 구간에서) |

- **청산 임계치**: 총점 ≥ 50점 시 청산 신호

### B. 리스크 기반 청산 (우선순위 순)

1. **고정 손절**: -0.5% 도달 시 즉시
2. **시간 제한**: 설정 시 (현재 비활성)
3. **+2% 선익절**: 에너지 부족 시
4. **트레일링 스탑**: 최고가 대비 -0.6%
5. **확장 익절**: +3.5% 도달

---

## 🎯 7. 권장 조정 방향

### 현재 문제점 분석

1. **진입 난이도**
   - 130점 진입 조건이 다소 높을 수 있음
   - GRASSUSDT 같은 변동성 높은 코인에서는 진입 기회 부족 가능

2. **목표 수익률**
   - +2%/+3.5%는 레버리지 50배 기준 적정
   - 트레일링 0.6%는 안정적이나 일부 수익 포기 가능

### 조정 제안

#### 제안 A: 진입 조건 완화 (더 많은 기회)
```python
min_entry_score: float = 90.0      # 100 → 90
strong_entry_score: float = 120.0  # 130 → 120
instant_entry_score: float = 150.0 # 160 → 150
```

#### 제안 B: 목표 수익률 상향 (큰 수익)
```python
tp_primary_pct: float = 0.025      # 2.5% (2.0% → 2.5%)
tp_extended_pct: float = 0.04      # 4.0% (3.5% → 4.0%)
```

#### 제안 C: 트레일링 거리 축소 (수익 극대화)
```python
trailing_stop_pct: float = 0.004   # 0.4% (0.6% → 0.4%)
```

#### 제안 D: 확장 익절 조건 완화
```python
extended_energy_score_threshold: float = 120.0  # 130 → 120
```

---

## 📝 8. 변경 시 주의사항

### 1. 백테스트 필수
- 설정 변경 후 반드시 과거 데이터로 백테스트 실행
- 최소 1주일~1개월 데이터 검증

### 2. 코인별 특성 고려
- **고변동성 코인** (GRASSUSDT 등): 진입 조건 완화, 트레일링 거리 확대
- **저변동성 코인** (BTCUSDT 등): 현재 설정 유지 또는 목표 수익률 축소

### 3. 레버리지 영향
- 레버리지 50배 기준: +2% = +100% 수익
- 손절 -0.5% = -25% 손실
- 레버리지 변경 시 비율 재조정 필요

### 4. 재시작 필요
```python
# 설정 변경은 런타임 중 적용 안됨
print(f"[{self.engine_name}] ⚠️  설정 변경은 재시작 후 적용됩니다")
```
- 엔진 중지 → 설정 수정 → 엔진 재시작

---

## ✅ 9. 검증 완료 사항

### 코드 레벨
- ✅ SignalEngineConfig 확인: `min_entry_score=100`, `strong_entry_score=130`, `instant_entry_score=160`
- ✅ RiskManagerConfig 확인: `tp_primary_pct=0.02`, `tp_extended_pct=0.035`, `trailing_stop_pct=0.006`
- ✅ Alpha/Beta/Gamma 모두 동일한 Orchestrator 사용
- ✅ 설정 위치 명확히 식별됨

### 로직 검증
- ✅ 진입 점수 체계 (최대 170점)
- ✅ 선익절 → 확장 익절 로직 구현
- ✅ 트레일링 스탑 활성화 조건 (+1%)
- ✅ 최소 수익 확정 로직 (+2%)

---

## 🚀 다음 단계

1. **현재 설정 검토**: 위 보고서 내용 확인
2. **조정 필요성 판단**: 백테스트 결과 또는 실거래 성과 기반
3. **설정 변경 계획 수립**: 제안 A~D 중 선택 또는 커스텀
4. **백테스트 실행**: 변경된 설정으로 검증
5. **단계적 적용**: Alpha → Beta → Gamma 순차 적용

---

**보고서 작성 완료**  
전략 진입/청산 조건이 정확하고 올바르게 확인되었습니다.
